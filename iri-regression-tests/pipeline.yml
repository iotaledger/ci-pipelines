steps:
  - name: Step 1
    command:
      - echo 'aaaa' >> test1.txt
      - pwd 
      - ls -al
    plugins:
      docker-compose#v2.5.1:
        run: app
        volumes:
          - './workdir:/workdir'
        config:
          - ci-pipelines/iri-regression-tests/docker-compose.yml
        leave-volumes: true
    agents:
      queue: sdj



  - name: Step 2
    command:
      - pwd 
      - ls -al
      - cat test1.txt
    plugins:
      docker-compose#v2.5.1:
        run: app
        volumes:
          - './workdir:/workdir'
        config:
          - ci-pipelines/iri-regression-tests/docker-compose.yml
    agents:
      queue: sdj