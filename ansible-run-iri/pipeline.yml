steps:
  - name: Step 1
    command: ansible-playbook 
    plugins:
      docker#v2.0.0:
        image: "sadjy/ansible-run" 
        shell: "/bin/sh -e -c" 
        environment:
          - SSH_CLIENT_SIGNER_ADDR
          - SSH_HOST_SIGNER_ADDR
          - DOCKER_VAULT_TOKEN
    agents:
      queue: ops
